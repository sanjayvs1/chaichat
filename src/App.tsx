// This component is no longer used as the main app component
// TanStack Router is now handling routing via src/main.tsx
// The main content has been moved to src/routes/index.tsx

export default function App() {
  return (
    <div>
      <p>This component is no longer used. TanStack Router is now handling routing.</p>
    </div>
  )
}
